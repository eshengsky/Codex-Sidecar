## 一、项目基础约束

### 项目背景

- 本项目是基于 electron 的桌面版应用。
- 不需要考虑任何浏览器兼容性。

### 包管理

- 项目统一使用 `pnpm` 作为包管理器。
- 禁止使用 `npm`、`yarn` 或其他包管理器。
- 安装、升级、移除依赖前，必须先说明原因并等待确认。
- 不要手动直接修改 lockfile，必须通过包管理器的安装来自动生成。

### Vue 单文件组件规范

- Vue 单文件组件必须按照以下顺序组织：

```vue
<template></template>

<script></script>

<style></style>
```

- 避免使用 `class` 组织业务脚本。
- 优先使用模块化函数、组合式函数、工具函数拆分逻辑。
- 修改现有组件时，优先保持原有代码风格和组织方式。

### 组件和样式

- 组件库统一使用 `@nuxt/ui`。
- 样式库使用 tailwindcss，除非有必要否则避免使用 style。

## 二、修改与确认流程

### 修改流程

- 除非是非常简单的代码修改任务，否则必须等我明确回复“开始改”后，才能开始修改代码。
- 如果我没有明确要求你修改代码，只能输出：
  - 方案；
  - 待确认问题。
- “非常简单的代码修改任务”仅包括：
  - 文案修改；
  - 明显 typo 修复；
  - 单个样式值调整；
  - 不涉及业务逻辑、接口、权限、路由、组件结构的轻微修改。
- 如果不确定任务是否属于“非常简单”，一律按非简单任务处理。
- 禁止擅自扩大修改范围。
- 禁止顺手重构与当前需求无关的代码。
- 有任何不清楚、不确定、缺少上下文的地方，必须主动向我确认。
- 不要启动本地开发服务、不要执行编译、不要做 ESLint、tsc 检查。
- 不需要打开页面做验证，我会自行验证。

## 三、UI 与交互规范

### 基础交互

- 空数据场景尽量使用项目内已有的空数据组件。
- 禁止使用 `window.alert`、`window.prompt`、`window.confirm`、`select` 等浏览器原生组件。
- 必须使用项目组件库提供的弹窗、确认框、提示组件。
- 只有 icon、没有文字的按钮，通常需要增加 Tooltip 说明，但特别常见且语义明确的按钮可以例外，例如关闭按钮。
- 样式避免使用 grid，尽量用 flex。
- nuxt-ui 组件的颜色统一使用 neutral，有特别要求的除外。

### 加载与反馈

- 响应较慢或影响主流程的关键接口，应提供合适的加载反馈，例如按钮 loading、局部 loading、骨架屏或占位状态。
- 列表、详情、看板、统计卡片等首屏或主体内容加载时，应优先使用项目内已有的 loading、骨架屏或占位组件。
- 加载反馈应尽量局部化，避免无必要地阻塞整个页面。
- 对点赞、收藏、关注、开关切换等轻量操作，不应默认使用阻塞式 loading。
- 轻量操作应优先考虑即时 UI 反馈、乐观更新、禁用重复点击、失败回滚或轻提示。
- 是否使用 loading 应结合接口耗时、业务重要性、失败成本和用户是否需要等待结果来判断。

### 非幂等操作与重复提交

- 对创建、提交、保存、删除、审批、发布、导入、导出、生成等非幂等操作，必须考虑重复触发问题。
- 关键写操作不能只依赖防抖处理，应优先使用：
  - 按钮 `loading` 状态；
  - 按钮 `disabled` 状态；
  - 请求进行中的状态锁；
  - 项目内已有的防重复提交工具函数或 hooks。
- 请求结束后必须正确恢复 `loading` / `disabled` 状态，异常、失败、取消场景也要处理。
- 不要为了防重复提交影响正常交互，例如搜索输入、筛选联想等高频交互应按场景使用防抖或节流。

## 四、接口、权限与异步状态

### 接口、权限与数据

- 不要猜测接口字段、枚举值、状态值、权限 code、路由参数或业务含义。
- 修改涉及接口数据时，应确认字段来源、空值情况和异常情况。
- 修改涉及权限控制时，应确认权限来源、判断逻辑和影响范围。
- 修改涉及表单时，应确认校验规则、默认值、提交格式和回显格式。
- 对接口返回为空、异常、加载中等状态，应按项目已有模式处理。

### 异步请求竞态

- 涉及 Tab 切换、搜索、筛选、分页、排序、详情切换、级联选择等会频繁触发异步请求的场景，必须考虑请求竞态问题。
- 不允许后返回的旧请求覆盖当前页面状态。
- 应根据项目已有模式优先采用以下方式处理：
  - 请求取消，例如 `AbortController`、axios cancel token 或项目封装的取消能力；
  - 请求序号 / request id 校验，只允许最新请求更新状态；
  - 响应数据与当前条件二次校验，例如当前 Tab、筛选条件、分页参数仍然匹配时才更新列表；
  - 组件卸载或页面离开时清理未完成请求。
- 切换 Tab、搜索条件、分页参数后，列表的 `loading`、空状态、错误状态必须与当前请求条件对应。
- 禁止在未确认当前上下文仍然有效的情况下，直接用异步响应覆盖页面状态。

## 五、性能与大数据量渲染

### 大数据量渲染

- 对可能存在大量数据的列表、表格、树、选择器、下拉选项、级联面板等场景，必须评估渲染性能。
- 不要在页面中一次性渲染明显可能过大的数据集。
- 应优先参考项目内已有的大数据量渲染方案，例如：
  - 虚拟列表；
  - 虚拟表格；
  - 懒加载树节点；
  - 分页加载；
  - 分批加载；
  - 服务端搜索；
  - 服务端分页。
- 是否使用虚拟列表，应结合组件库能力、交互需求、数据规模、固定高度/动态高度、选择状态、展开状态等因素判断。
- 如果组件库已有虚拟滚动能力，应优先使用组件库提供的能力，不要自行实现。
- 如果无法确定数据规模，必须向我确认，不要直接一次性渲染全部数据。

## 六、样式与可维护性

### 样式规范

- 优先复用项目内已有样式、变量、mixin、设计 token 和布局模式。
- 不要随意新增全局样式。
- 不要随意使用内联样式，除非项目内已有类似模式或确有必要。

### 注释规范

- 复杂业务逻辑、非显然实现、特殊边界处理、兼容历史逻辑、临时方案等代码，必须添加必要注释说明原因。
- 注释应解释“为什么这样做”或“这个分支处理什么业务场景”，不要重复描述代码本身在做什么。
- 不要添加无意义注释，例如“设置变量”“调用接口”“遍历列表”等显而易见的说明。
- 如果代码依赖特定接口行为、权限规则、历史数据格式或产品约定，应在注释中简要说明。
- 临时方案必须标明原因，并尽量关联后续清理条件或 TODO。
- 修改已有复杂逻辑时，应保留有价值的原注释；如果逻辑变化导致注释失效，必须同步更新注释。

### 文档说明

- 对于非常复杂、跨模块、长期维护成本高或容易被误改的业务/技术实现，仅靠代码注释不足以说明时，应考虑补充独立文档。
- 独立文档适用于：
  - 复杂业务流程、状态流转、权限规则；
  - 多模块协作逻辑；
  - 复杂表单、审批流、配置规则；
  - 特殊接口约定、数据映射、历史兼容逻辑；
  - 复杂性能优化方案；
  - 有长期维护价值的技术方案说明。
- 不要为简单功能、普通组件、小范围样式调整创建额外文档。
- 新增文档前应先检查项目内是否已有文档目录或路径约定。
- 如果项目内没有更明确的约定，默认将文档放在项目根目录的 `docs/` 下。
- 新增文档前必须说明文档用途、建议路径和主要内容，并等待确认。
- 文档内容应说明背景、规则、关键流程、边界情况和维护注意事项，避免只罗列代码实现。

### 代码质量

- 优先复用项目内已有工具函数、hooks、组件和类型定义。
- 不要为了单个需求引入不必要的新抽象。
- 不要引入无关依赖。
- 不要使用未确认的 API、字段名、路由、接口参数或权限标识。
- 保持修改范围最小，确保改动与当前需求直接相关。
- 修改共享组件、公共方法、通用样式时，必须说明可能影响的范围。

## 七、问题定位与修复

- 定位 bug 时禁止直接靠猜测修改代码，必须先尽量获取可验证的证据。
- 修复前应优先明确：
  - 问题的复现步骤；
  - 实际表现与预期表现；
  - 影响范围；
  - 最近相关改动；
  - 控制台错误、接口异常、请求参数、响应数据等关键信息。
- 如果运行环境支持查看浏览器开发者工具，应优先查看：
  - Console 错误与警告；
  - Network 请求 URL、Method、Status、请求参数、响应体；
  - 接口耗时、失败原因、重试情况；
  - 前端路由、页面状态、组件状态变化。
- 如果无法直接查看 Console 或 Network 信息，应主动向我索要相关截图、日志、HAR 文件、接口返回或复现录屏。
- 在缺少运行时信息且仅靠静态代码无法判断问题原因时，必须说明当前缺少哪些证据，不要假装已经确认原因。
- 可以提出一个或多个可能原因，但必须明确标注为“推测”，并说明需要什么证据来验证。
- 如果确实需要通过临时日志辅助定位，可以建议插入临时 `console.log`、断点信息或调试标记，但必须先说明插入位置、目的和要观察的数据，并等待确认。
- 临时调试代码必须在问题定位后移除，禁止遗留 `console.log`、`debugger`、临时 mock、临时分支或无关测试代码。
- 修改 bug 时应尽量先定位根因，再做最小范围修复，避免用兜底逻辑掩盖真实问题。
- 修复后应说明验证方式；如果无法完整验证，必须说明未验证的部分和残余风险。
